# config-pathway-groups.R, outcome groups analyzed with the Pathway module.
# Trenton's rule: primary -> ORA (enrichment); secondary/tertiary/exploratory ->
# Pathway Analysis. Pathway runs filter OFF (no reference metabolome needed).
PATHWAY_GROUPS <- c("secondary", "tertiary")
